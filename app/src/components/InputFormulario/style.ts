
import { StyleSheet } from 'react-native';
import { color } from '../../util/color';

export const styles = StyleSheet.create({
    formulario: {
        backgroundColor: "#fff",
        padding: 10,
        paddingVertical: 12,
        paddingLeft:15,
        width: "100%",
        borderBottomWidth: 1,
        borderBottomColor: color.cinza2,
        color: color.cinza2
    }
});
