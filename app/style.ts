
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    description: {
        marginHorizontal: '15%',
        marginBottom: 10,
        textAlign: 'justify',
        fontSize: 14
    }
});
